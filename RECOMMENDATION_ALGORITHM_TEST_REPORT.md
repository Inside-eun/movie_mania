# 추천 알고리즘 A/B 테스트 방법론 보고서

`/test` 프로토타입에서 "같은 감독 / 같은 장르 / 같은 배우 / 같은 개봉년도" 네 가지 추천 알고리즘 중 어떤 방식이 실제 클릭(관객 선호)으로 더 많이 이어지는지 검증하기 위한 테스트 환경을 어떻게 구성했는지, 그리고 결과의 신빙성을 확보하기 위해 어떤 설계 결정을 했는지 정리한다.

## 1. 테스트 목적

같은 영화 상세 페이지 하단에 "함께 보면 좋은 작품"을 4가지 추천 기준(감독/장르/배우/개봉년도) 중 하나로 무작위 노출했을 때, 어떤 기준이 실제 클릭률(선호도)이 높은지 비교한다. 오늘 상영 스케줄에 매인 실 서비스와 분리된 `/test` 환경에서, 상영 여부와 무관하게 넓은 영화 풀을 대상으로 실험한다.

## 2. 시스템 구성

| 영역 | 경로 | 역할 |
|---|---|---|
| 목록 페이지 | `src/app/test/page.tsx` | 카탈로그 전체를 그리드로 노출, 클릭 시 상세로 이동 |
| 상세 페이지 | `src/app/test/movie/[slug]/page.tsx` | 리뉴얼 상세 UI + 추천 섹션 |
| 추천 로직 | `src/lib/similarMovies.ts` | 4개 알고리즘 중 하나를 선택해 최대 4편 반환 |
| 카탈로그 | `src/mock/testCatalog.ts` | 영화 풀 구성 |
| 메타데이터 | `src/mock/movieMeta.json` (+ `movieMeta.ts`) | 감독/장르/출연진/개봉년도/평점 |
| 클릭 기록 | `src/lib/analyticsStore.ts`, `src/app/api/analytics/track/route.ts` | 클릭 이벤트 저장·집계·초기화 |
| 결과 확인 | `src/app/test/stats/page.tsx` (`/test/stats`) | 알고리즘별/영화별 집계, 전체 초기화 |

## 3. 데이터 구성

- **오늘 상영 스케줄**: `src/mock/movies-snapshot.json` 기준 중복 제거 48편
- **TMDB 평점 top 100**: `src/scripts/buildTopMovies.cjs`가 TMDB `/movie/top_rated`를 5페이지(100편) 수집, 각 작품의 `/movie/{id}?append_to_response=credits`로 감독·출연진(상위 5명)·장르·개봉일·평점을 채움
- 두 소스를 제목 기준 중복 제거해 병합 → **총 147편** 카탈로그 (`movieMeta.json` 145건, 매칭 실패 2편은 스냅샷 자체 포스터로 폴백)
- 메타데이터 수집 스크립트: `src/scripts/buildMovieMeta.cjs`(스냅샷 48편), `src/scripts/buildTopMovies.cjs`(top 100편) — 둘 다 재실행 가능한 일회성 빌드 스크립트로, 카탈로그를 키울 때 다시 돌리면 됨

## 4. 추천 알고리즘 선택 로직

`pickSimilarMovies(sourceTitle)` (`src/lib/similarMovies.ts`):

1. 감독/장르/배우/개봉년도 4개 리졸버를 모두 평가해 **매칭 가능한 알고리즘만** 후보로 추림
2. 후보 중 하나를 아래 가중치로 무작위 선택
3. 선택된 기준으로 매칭된 작품들을 무작위 셔플 후 최대 4편 반환

## 5. 신빙성 확보를 위한 설계 결정

### 5.1 알고리즘 노출 빈도 균등화 (핵심)

**문제 발견**: 147편 카탈로그를 시뮬레이션한 결과, 알고리즘별로 "매칭 가능한 영화 비율"이 크게 달랐다.

| 알고리즘 | 매칭 가능 비율 |
|---|---|
| 장르 | 145/147편 (98.6%) |
| 개봉년도 | 120/147편 (81.6%) |
| 배우 | 56/147편 (38.1%) |
| 감독 | 45/147편 (30.6%) |

장르·개봉년도는 대부분의 영화에 걸리는 넓은 속성이고, 감독·배우는 147편 규모에서 겹치는 경우가 적은 좁은 속성이다. 매칭 가능한 알고리즘 중 **균등 확률로 추첨**하는 초기 버전으로 시뮬레이션한 결과:

| 알고리즘 | 보정 전 노출 비율 |
|---|---|
| 장르 | 43.8% |
| 개봉년도 | 33.9% |
| 배우 | 12.7% |
| 감독 | 9.5% |

→ 장르 하나가 거의 절반을 차지해, 이 상태로 클릭 수를 비교하면 "장르 알고리즘이 더 선호된다"가 아니라 "장르 알고리즘이 4배 더 자주 노출됐다"를 관측하게 될 위험이 컸다. 즉 **노출 편향이 클릭 편향으로 오인될 소지**가 있었다.

**보정 방법**: 알고리즘별 가중치를 `(카탈로그 크기 / 매칭 가능 편수)²`로 계산해, 희소한 알고리즘일수록 매칭 가능할 때 더 높은 확률로 뽑히도록 함(`getAlgorithmWeights()` / `pickWeighted()`).

| 알고리즘 | 가중치 |
|---|---|
| 감독 | 10.67 |
| 배우 | 6.89 |
| 개봉년도 | 1.50 |
| 장르 | 1.03 |

**보정 후 시뮬레이션** (영화당 50회 시행, 총 7,250회):

| 알고리즘 | 보정 후 노출 비율 |
|---|---|
| 개봉년도 | 32.2% |
| 장르 | 26.8% |
| 배우 | 20.8% |
| 감독 | 20.2% |

8.3%p~43.8%p 편차였던 것을 20.2%p~32.2%p 범위로 좁혔다. 완전한 25/25/25/25는 아닌데, 이는 로직의 한계가 아니라 **감독·배우 정보 자체가 147편 규모에서 구조적으로 희소하기 때문**이다(§7 한계 참고).

### 5.2 노출 순서 편향 제거

목록 페이지가 "오늘 상영작을 먼저, TMDB 인기작을 나중에" 순서로 나열하면 위쪽에 노출된 영화가 더 많이 클릭되는 위치 편향이 생길 수 있다. `testCatalog.ts`는 제목을 해시(`hashTitle`)해 그 값으로 정렬함으로써 두 소스를 뒤섞어 노출한다. 진짜 `Math.random()`을 쓰지 않고 **결정적(deterministic) 해시 정렬**을 쓴 이유는, `/test` 페이지가 서버 렌더링 후 클라이언트에서 하이드레이션되는 Next.js 클라이언트 컴포넌트라 요청마다 순서가 달라지면 서버/클라이언트 렌더 결과가 어긋나는 하이드레이션 불일치가 발생하기 때문이다. 해시 정렬은 뒤섞인 것처럼 보이면서도 항상 같은 순서를 재현한다.

### 5.3 출처 정보 은닉

카탈로그에 "오늘 상영중"인 영화와 "TMDB 인기작"인 영화가 섞여 있는데, 이 구분을 화면에 노출하면 사용자가 "예매하려고" 혹은 "실존 여부 확인 차 클릭"하는 식으로 추천 알고리즘과 무관한 이유로 클릭할 수 있다(혼입 변수). `CatalogMovie` 타입에서 `theater`/`time` 필드를 아예 제거하고, 목록·상세 어디에도 상영중/TMDB 여부를 표시하지 않도록 해서 이 혼입 변수를 제거했다.

### 5.4 클릭 이벤트 설계

`analyticsStore.ts`가 기록하는 이벤트는 두 종류:

- `list_click`: 메인 목록에서 어떤 영화를 클릭했는지
- `recommendation_click`: 추천 섹션에서 `{algorithm, sourceTitle, targetTitle}` — 어떤 알고리즘이 골라준 어떤 영화를 클릭했는지

알고리즘별 집계(`byAlgorithm`)와 영화별 집계(`recommendationClicks`)를 분리해서, "어떤 알고리즘이 잘 먹히는지"와 "어떤 영화가 잘 먹히는지"를 따로 볼 수 있게 했다.

### 5.5 공유 저장소로 데이터 일관성 확보

Vercel 배포 시 서버리스 함수 인스턴스가 여러 개 뜰 수 있어, 인스턴스별 로컬 파일에 기록하면 통계가 흩어진다. `UPSTASH_REDIS_REST_URL`/`TOKEN`이 설정된 경우(Production/Preview에 이미 연결됨) Redis 리스트(`rpush`/`lrange`)에 기록해 모든 인스턴스가 같은 데이터를 보게 했다. 로컬 개발처럼 Redis가 없는 환경에서는 `.cache/test_analytics.json` 파일로 자동 폴백한다. `/test/stats`에 현재 어느 저장소를 쓰는지 표시해 이 부분이 눈에 보이게 했다.

### 5.6 초기화 가능

테스트를 여러 라운드로 나눠 돌리거나(예: 1차 테스트 후 로직 수정, 2차 테스트) 노이즈성 클릭(오클릭, QA 테스트 클릭)을 걷어내고 싶을 때를 위해 `/test/stats`에 "전체 초기화" 버튼을 두었다. `DELETE /api/analytics/track` → `clearEvents()`가 Redis 키 삭제 또는 파일 초기화를 수행한다. 확인 다이얼로그를 거치도록 해 실수 클릭으로 데이터가 날아가지 않게 했다.

## 6. 결과를 읽는 방법

`/test/stats`에서 확인 가능한 지표:

- **알고리즘별 클릭 수**: 각 알고리즘이 추천되어 클릭된 총 횟수
- **추천작 중 많이 클릭된 영화**: `{알고리즘, 영화}` 조합별 클릭 수 상위 10개
- **메인 목록에서 많이 클릭된 영화**: 카탈로그 자체의 인기도(알고리즘과 무관)

## 7. 한계와 유의사항

솔직하게 짚어야 할 부분:

1. **클릭만 기록하고 노출(impression)은 기록하지 않는다.** §5.1에서 노출 비율을 20.2~32.2%로 맞췄지만 완전히 동일하진 않다. 따라서 `byAlgorithm`의 **원시 클릭 수를 그대로 비교하면 여전히 왜곡될 수 있다.** 엄밀하게 비교하려면 클릭 수를 §5.1의 노출 비율로 나눠 정규화하거나(예: 개봉년도 클릭 수 ÷ 0.322), 노출 이벤트도 별도로 기록해 실제 클릭률(CTR = 클릭/노출)로 비교하는 것이 좋다. 노출 트래킹은 아직 구현되어 있지 않다.
2. **147편은 감독·배우 매칭 표본으로는 작다.** 카탈로그가 커질수록(특히 같은 감독·배우의 여러 작품이 늘어날수록) 가중치 없이도 자연스럽게 균형이 맞을 수 있다. 지금의 가중치 보정은 이 카탈로그 크기에서의 경험적 보정이며, 카탈로그가 바뀌면(`buildTopMovies.cjs` 재실행 등) 가중치도 자동으로 재계산된다(`getAlgorithmWeights()`가 카탈로그 기준으로 매번 산출).
3. **카탈로그가 실제 상영 스케줄을 대표하지 않는다.** TMDB 평점 top 100은 오늘 볼 수 있는 영화가 아니라 역대 고평점작 위주라, 실 서비스 사용자 취향과 다를 수 있다.
4. **통계적 유의성 검정은 하지 않는다.** 지금은 단순 카운트 비교이며, 표본이 적을 때는 카이제곱 검정 등으로 우연에 의한 차이인지 확인하는 절차가 없다.
5. **Redis 미설정 환경(로컬 개발, Vercel Development)에서는 인스턴스별 파일 저장소를 쓰므로**, 같은 브랜치를 여러 환경에서 테스트하면 집계가 나뉠 수 있다. Production/Preview는 이미 Redis가 붙어 있어 문제없다.

## 8. 재현 방법

```bash
# 메타데이터 재수집 (카탈로그를 바꾸거나 늘릴 때)
node src/scripts/buildMovieMeta.cjs   # 오늘 상영작 48편
node src/scripts/buildTopMovies.cjs   # TMDB 평점 top 100편

# 로컬 실행
npm run dev
# http://localhost:3000/test        목록/추천 테스트
# http://localhost:3000/test/stats  결과 확인 · 초기화
```

## 9. 향후 개선 아이디어

- 노출(impression) 이벤트 기록 → CTR 기반 비교로 전환
- 카이제곱 검정 등으로 알고리즘 간 클릭률 차이의 통계적 유의성 확인
- 사용자(세션) 단위 식별을 추가해 동일 사용자의 반복 클릭이 결과를 왜곡하지 않는지 확인
- 카탈로그 확장(TMDB `popular`, 장르별 top 등 추가) 시 감독/배우 알고리즘의 자연스러운 매칭률 재측정
